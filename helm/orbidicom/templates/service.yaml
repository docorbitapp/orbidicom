apiVersion: v1
kind: Service
metadata:
  name: {{ include "orbidicom.fullname" . }}
  labels:
    {{- include "orbidicom.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "orbidicom.selectorLabels" . | nindent 4 }}
